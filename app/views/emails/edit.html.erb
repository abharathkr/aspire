<div class="container-fluid order">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <div class="well well-md">
        <fieldset>
          <legend class="text-center">Editing Email</legend>
			     <%= render 'form', email: @email %>
        </fieldset>
        <%= link_to 'Show', @email %> |
        <%= link_to 'Back', emails_path %>
      </div>
    </div>
  </div>
</div>

